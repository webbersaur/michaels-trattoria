# Resend — private feedback form (/api/contact)
# Uses the shared Webbersaurus Resend account, whose webbersaurus.com sender domain is
# already verified, so no per-site DNS is needed.
#
# Set RESEND_API_KEY in the Vercel project "michaels" (Production + Preview).
# The other two have working defaults in api/contact.js and are optional overrides.

RESEND_API_KEY=
# CONTACT_TO_EMAIL=michaelstrattoria@att.net
# CONTACT_FROM_EMAIL=Michael's Trattoria <noreply@webbersaurus.com>
